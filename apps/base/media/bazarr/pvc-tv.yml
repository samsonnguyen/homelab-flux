apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bazarr-shared-tv
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 8Ti
  volumeMode: Filesystem
  volumeName: bazarr-shared-tv
