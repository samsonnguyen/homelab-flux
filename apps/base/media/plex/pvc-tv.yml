apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plex-shared-tv
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 8Ti
  volumeMode: Filesystem
  volumeName: plex-shared-tv
