apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sabnzbd-shared
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 8Ti
  volumeMode: Filesystem
  volumeName: sabnzbd-shared
